/**
 * useTheme hook for accessing and manipulating the theme.
 * 
 * Provides a convenient interface for components to:
 * - Access current theme state
 * - Change theme mode
 * - Toggle between light/dark
 * 
 * Re-exports the useTheme hook from theme/useTheme for backward compatibility.
 */

export { useTheme, type UseThemeReturn } from '../theme/useTheme';
